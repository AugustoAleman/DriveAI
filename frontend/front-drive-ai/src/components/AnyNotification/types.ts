export interface ColorProps {
    colorIcon?: string;
}

export interface AnyNotificationProps {
    title?: string;
    text?: string;
    icon?: React.ReactNode;
    buttonTitle?: string;
    colorIcon?: string;
}